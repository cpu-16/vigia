# Procedencia del código

Este archivo cumple el **artículo 11.c** de los Términos y Condiciones: «Toda base preexistente
utilizada debe declararse en el archivo README del repositorio, con indicación de su origen.»

## Ventana de competencia

Del **9 de septiembre de 2026 a las 8:00** al **11 de septiembre a las 8:00**, hora de Panamá.
Todo el código de `src/`, `app/` y `fixtures/` se escribió dentro de esa ventana. Cada fila de la
tabla siguiente apunta al commit que lo introdujo.

## Referencia previa declarada

Librería propia de experimentación con el SDK de QVAC, escrita por el equipo entre el 3 y el 8 de
septiembre de 2026 (pruebas de recuperación con citas, extracción bajo gramática JSON, sellado
Ed25519, delegación P2P por llave pública, mediciones de rendimiento en laptop y teléfono).
**Uso en este repositorio: referencia de diseño y de resultados medidos. No se copió código.**
Disponible al jurado a solicitud.

## Componentes de este repositorio

| Componente | Archivos | Qué hace | Relación con la referencia | Commit |
|---|---|---|---|---|
| Registro de rendimiento | `src/core/rendimiento.js` | Una línea JSON por carga y por inferencia: modelo, hardware, origen local/delegado, carga, tokens, TTFT, throughput | Nuevo. La referencia solo guardaba el último resumen en memoria | 9-sep |
| Runtime QVAC común | `src/core/runtime.js`, `src/core/runtime.test.js` | Carga (local o delegada por llave pública con respaldo local) y completion medida sobre `@qvac/sdk` 0.18.2; el modo de ejecución se registra según lo que pasó (`getLoadedModelInfo`), no según lo pedido | Nuevo. Toma de la referencia el dato de que `gpu_layers`/`ctx_size` son las claves de `modelConfig` y que Qwen3 antepone `<think>` | 9-sep |
| Sellado de actas | `src/core/sello.js` | JSON canónico + SHA-256 + firma Ed25519 con la llave del nodo (crypto de Node, sin dependencias); `verificar()` funciona con la llave pública que viaja en el sello | Nuevo. La referencia tenía un sellado propio; aquí se reescribió con canonización recursiva y sin librerías | 9-sep |
| Almacén de eventos encadenado | `src/core/eventos.js`, `src/core/sello-eventos.test.js` | JSONL solo-agregar; cada evento lleva la huella del anterior; `verificarCadena()` señala el primer evento alterado | Nuevo | 9-sep |
| Verificador de actas en el navegador | `app/verificar.html`, `app/verificar.js`, `src/core/verificar-web.test.js` | Rehace con WebCrypto lo mismo que el sellador de Node: canónico, SHA-256 del cuerpo sin el sello y firma Ed25519 sobre los BYTES del hash, sin una sola llamada al servidor. Acepta un acta suelta, una lista o un JSONL encadenado; resume los campos de primer nivel sin conocer el esquema; y trae un botón que cambia un carácter para enseñar que la firma lo detecta | Nuevo. La referencia tenía una página de verificación con WebCrypto; esta se reescribió desde cero y la lógica se sacó a un módulo que `node --test` ejecuta contra actas selladas de verdad | 9-sep |
| Contrato de equipos | `src/equipos/esquema.js` | Listas de referencia del reto (modalidades, marcas, estados, confianzas), sinónimos es/en/pt, numerales en tres idiomas, esquema JSON con las citas primero y prompt del sistema con ejemplo | Nuevo. De la referencia se tomó el hallazgo medido de que poner la evidencia antes que el campo mejora al 1.7B bajo gramática; el esquema y el prompt se rediseñaron (campos `*_quote`) porque con `evidence.quantity` el modelo contestaba «Yes» | 9-sep |
| Extracción verificada | `src/equipos/extraer.js` | El modelo propone bajo gramática; el código verifica cada dato contra el reporte y lo marca `cita` / `texto` / descartado; país por respaldo determinista; edad citada pero no rellenada se completa desde la cita | Nuevo | 9-sep |
| Detector de menciones | `src/equipos/menciones.js` | Numeral + tipo de equipo en la misma cláusula (es/en/pt); completa lo que el modelo omite («one MR and two CTs» → el CT que faltaba) marcado como verificado por texto | Nuevo | 9-sep |
| Reglas del reto | `src/equipos/reglas.js` | Confianza y estado por campo según la hoja «Agent Question Logic», año de instalación, y la siguiente pregunta útil (una a la vez, sin repetir) | Nuevo, escrito desde la hoja del xlsx | 9-sep |
| Duplicados explicables | `src/equipos/duplicados.js` | Fellegi-Sunter a mano: pesos log2(m/u) por campo declarados, nulos aportan 0, probabilidad y veredicto mismo/revisar/nuevo con la explicación campo por campo; nunca fusiona sola | Nuevo (método de splink, sin librería) | 9-sep |
| Pruebas de equipos | `src/equipos/equipos.test.js` | 6 pruebas puras + los 10 prompts oficiales del xlsx, uno en español y uno en portugués contra el modelo real (12/12 el 9-sep, 1.4-3.3 s cada uno en la RTX 4060) | Nuevo | 9-sep |
| Dictado por voz | `src/core/voz.js` | Audio del navegador → WAV 16 kHz normalizado (highpass + dynaudnorm) → Whisper por QVAC; guarda de silencio por pico en dBFS y registro con factor de tiempo real | Nuevo. De la referencia se tomó el dato medido de que el audio del HONOR llega a −22 dB y hay que normalizar antes de transcribir | 9-sep |
| Base instalada | `src/equipos/almacen.js`, `src/equipos/almacen.test.js` | Observaciones inmutables encadenadas; el inventario se calcula consolidando por identidad de activo (Fellegi-Sunter); Confirmed solo con dos observadores distintos y sin sumar unidades; Customer 360, agregados por país/ciudad/modalidad, renovaciones con la regla COCIR (≤5 al día, 6-10 planificar, >10 reemplazar) e información incompleta | Nuevo | 9-sep |
| Servidor del nodo | `src/servidor.js` | Sirve la app y expone el flujo: dictar, extraer con preguntas y duplicados, guardar sellando el acta, Customer 360, agregados, renovaciones, verificación y evidencia técnica. Ninguna ruta sale a un servicio externo | Nuevo | 9-sep |
| App instalable (PWA) | `app/index.html` (con lectura de placa y consulta en lenguaje natural desde el teléfono), `app/tablero.html`, `app/sw.js`, `app/manifest.webmanifest`, `app/icono.svg` | Captura por voz con onda real y tope de 90 s, o por texto; revisión con la evidencia subrayada sobre lo dicho, una pregunta a la vez, aviso de duplicado con su explicación, panel técnico por solicitud; cola en IndexedDB cuando no hay nodo; tablero con KPIs, agregados, renovaciones, Customer 360 y evidencia en vivo | Nuevo. Dirección de arte propia («libreta de campo clínica»): papel cálido, tinta azul profunda, turquesa solo para la acción, serif con carácter en los títulos | 9-sep |
| Guía sintética de sucursal | `fixtures/sucursal/guia-bpl.md`, `fixtures/sucursal/casos.json` | Guía operativa ficticia del «Banco Público del Litoral» (173 líneas: apertura, modo isla, arqueo, requisitos, datos personales, excepciones) y 20 consultas de prueba, 5 de ellas fuera de la guía | Nuevo, escrito hoy. Entidad, códigos y montos inventados; ninguna institución real | 9-sep |
| Procedimiento citado | `src/sucursal/guia.js`, `src/sucursal/procedimiento.js` | Búsqueda determinista por términos y códigos; el modelo elige el procedimiento por su CÓDIGO y el texto publicado lo pone el código desde la guía; se abstiene sin respaldo; el monto se verifica contra la cita o se toma de la sección solo si es la única cifra | Nuevo (Codex, integrado y medido) | 9-sep |
| Expediente de contingencia | `src/sucursal/expediente.js` | Máquina de estados abierto → en_proceso → cerrado reconstruida desde el JSONL encadenado; un caso cerrado no admite cambios; al cerrar emite el acta sellada verificable sin servidor | Nuevo (Codex) | 9-sep |
| Pruebas de sucursal | `src/sucursal/sucursal.test.js` | 3 pruebas puras (búsqueda, guardas, expediente con reinicio y detección de alteración) + los 20 casos contra el modelo real | Nuevo | 9-sep |
| Placas sintéticas | `fixtures/placas/generar.py`, `fixtures/placas/*.png`, `verdad.json` | 20 placas de identificación ficticias (10 nítidas + 10 con desenfoque, inclinación, reflejo u oscuridad) con los campos de la norma ISO 15223-1 y una cadena de elementos GS1 impresa; marcas y modelos son los ficticios del reto | Nuevo, generadas hoy con Pillow | 9-sep |
| Lectura de placa (track 02) | `src/equipos/placa.js`, `src/equipos/placa.test.js` | VisionPsy Nano 460M transcribe la placa línea por línea y las reglas interpretan: parser de cadena de elementos GS1 ((01) producto, (21) serie, (11) fabricación), campos con su origen y confianza, desacuerdo marcado cuando el código y la etiqueta difieren, y la identidad nunca se confirma sin una persona | Nuevo. De la referencia se tomó el hallazgo medido de que a VisionPsy hay que pedirle transcripción, no JSON | 9-sep |
| Proveedor de inferencia | `src/puente/proveedor.js` | Presta la GPU de un equipo a otros nodos por llave pública (Hyperswarm), con identidad estable por semilla guardada y cortafuegos opcional que solo admite consumidores autorizados; imprime cada solicitud delegada | Nuevo | 9-sep |
| Nodo puente del teléfono | `src/puente/nodo.js`, `src/puente/puente.test.js` | El SDK de QVAC corre dentro del teléfono y sirve la app al navegador del propio teléfono; delega al par autorizado cuando lo alcanza y responde con el modelo a bordo cuando no, avisándolo. Guarda propia para el hueco del SDK: una completion delegada que vuelve vacía se trata como par caído, porque `fallbackToLocal` solo cubre la carga | Nuevo | 9-sep |
| Consulta en lenguaje natural | `src/equipos/consulta.js`, `src/equipos/consulta.test.js` | El modelo traduce la pregunta a filtros bajo gramática y el CÓDIGO ejecuta la consulta sobre los datos estructurados: la respuesta no puede inventar un cliente que no está. Incluye la alerta de información no verificada recientemente | Nuevo. Es la meta que el reto pide con el ejemplo literal «clientes en Brasil con resonadores de más de siete años» | 9-sep |
| Recuperación semántica | `src/core/semantica.js`, `src/core/semantica.test.js` | Embeddings del SDK (EmbeddingGemma 300M) para recuperar por significado, y fusión por rango recíproco con la búsqueda por términos: no suma escalas distintas, un coseno alto no desplaza a un código exacto. Sin el modelo cargado, el sistema sigue funcionando solo con términos | Nuevo. El track 02 exige que las operaciones de RAG pasen por `@qvac/sdk`; los vectores los produce QVAC en el mismo equipo | 9-sep |
| Flujo DNS y detección | `src/red/consumidor.js`, `src/red/ventanas.js`, `src/red/deteccion.js` | Parser propio del query log de BIND9; reproducción del registro real COMO FLUJO respetando los intervalos; ventanas deslizantes por cliente y dominio; detección determinista por capas con lista blanca aprendida del propio tráfico y reglas para DGA, typosquatting, tunneling y beaconing | Nuevo (Codex, integrado) | 9-sep |
| Caso para el operador | `src/red/caso.js` | QVAC recibe solo los rasgos ya calculados, nunca el tráfico crudo, y redacta el caso; si menciona una cifra que no está en la evidencia se descarta y se usa un resumen determinista. El modelo explica, no decide | Nuevo (Codex) | 9-sep |
| Salidas operativas | `src/red/wazuh.js`, `src/red/qoe.js`, `infra/red/local_rules.xml`, `infra/red/ossec-localfile.xml`, `infra/red/grafana-panel.json` | Alertas en JSON-lines que Wazuh lee con `localfile`, con su regla propia; score de experiencia por zona con sus tres componentes visibles y cada fila marcando qué campos son sintéticos | Nuevo (Codex) | 9-sep |
| Ataques etiquetados | `fixtures/red/inyector.js` | Genera DGA, typosquatting, tunneling y beaconing etiquetados y deterministas por semilla, para poder medir precisión y recall sobre el tráfico real del patrocinador | Nuevo (Codex) | 9-sep |
| Sucursal en el navegador | `app/sucursal.html`, `src/sucursal/http.js`, `src/sucursal/http.test.js` | Las tres pantallas del cajero —consultar la guía, revisar el respaldo literal con su tope y sus pasos, documentar y cerrar con acta— sobre rutas HTTP que reutilizan el LLM ya cargado por el servidor. El navegador manda el identificador de una consulta que el nodo validó, nunca citas ni pasos: el texto que entra al expediente lo resuelve el servidor desde la guía. Incluye la guarda de alcance de `procedimiento.js` (un código válido no respalda una pregunta que la guía no cubre) y su medición en GPU | Nuevo | 9-sep |
| Política de respaldo entre pares | `src/puente/respaldo.js`, `src/puente/puente.test.js` | La guarda que el SDK no da, compartida por el teléfono y la laptop: `fallbackToLocal` solo cubre la CARGA, así que cuando el par muere con el modelo ya cargado la completion vuelve vacía y sin error. Se detecta por `ttft_ms == null` (el modelo no emitió un token) y no por el borrador, que la capa determinista de menciones rellena y disimula la caída; se recalcula en este nodo, se avisa a la persona y queda la fila `stage:'fallback'`. Tolera además el nodo sin modelo a bordo: sigue solo delegado y responde 503 en vez de morir | Nuevo. Sale de sacar a un módulo la política que ya vivía dentro de `nodo.js`; el detector se rehízo tras medir que el anterior no disparaba contra un par real | 9-sep |
| Segundo par QVAC en otra red | `evidencia/mac-par-9sep.md` | Medición de la laptop delegando a una Mac M5 Max que está en otra casa: portabilidad del producto (54 pruebas verdes en macOS/arm64), 0.65-1.17 s por captura a 255-280 tok/s contra 123-129 en la RTX, y la escena del par que se apaga antes y después del arreglo, con los dos huecos del SDK que hubo que tapar | Nuevo | 9-sep |

## Componentes de terceros

Ver `THIRD_PARTY.md`.
